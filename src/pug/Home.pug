doctype html
html(lang="ja")
    head
        include _temp_p_head
        title New_User
        link(rel="stylesheet", href="css/Style.css")
    body
      section 
        include _home-header
      section
        .story-wrapper
          .main-story.story
            a(href="#")
          .other-story.story
            a(href="#")
          .other-story.story
            a(href="#")
          .other-story.story
            a(href="#")
          .other-story.story
            a(href="#")
          .other-story.story
            a(href="#")
          .other-story.story
            a(href="#")
          .other-story.story
            a(href="#")
          .other-story.story
            a(href="#")
      section
        include _navbar
      include _temp_p_foot